{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "timeseries",
      "title": "Per-tenant Error Rate",
      "gridPos": { "h": 12, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "targets": [
        {
          "expr": "(\n  sum(rate(http_requests_total{http_status=~\"5..\"}[5m])) by (tenant_id)\n  or\n  sum(rate(http_requests_total[5m])) by (tenant_id) * 0\n)\n/\nsum(rate(http_requests_total[5m])) by (tenant_id)",
          "legendFormat": "{{tenant_id}}",
          "refId": "A"
        }
      ]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["txn-platform", "axiler"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "timezone": "browser",
  "title": "Per-tenant Error Rate",
  "uid": "dfy1yfv7kju9se",
  "version": 1
}
